"use client"

// This file is intentionally kept minimal — the main dashboard KPI cards
// are rendered directly inside dashboard-content.tsx using live state data.
// Keeping this export to avoid breaking the import.
export function SectionCards() {
  return null
}
